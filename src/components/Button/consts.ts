export enum ButtonTypes {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  TERTIARY = 'tertiary',
  ACTION = 'action',
  LINK = 'link'
}

export enum ButtonNativeTypes {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset'
}

export enum ButtonIconPosition {
  LEFT = 'left',
  RIGHT = 'right'
}
